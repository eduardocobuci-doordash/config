# Register zsh hooks - loaded last to ensure they persist
# This must load AFTER oh-my-zsh to avoid being overridden

if [[ -n "$ZSH_VERSION" ]]; then
  # Register the terminal title update hook
  autoload -Uz add-zsh-hook
  add-zsh-hook precmd update_title
fi

